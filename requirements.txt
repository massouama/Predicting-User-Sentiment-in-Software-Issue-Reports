# ===========================================================================
# Predicting User Sentiment in Software Issue Reports
# Python dependencies (tested with Python 3.11)
# ===========================================================================

# --- Core scientific stack ---
numpy>=1.26
pandas>=2.0
scipy>=1.11

# --- Machine learning ---
scikit-learn>=1.4          # classifiers, pipelines, CV, PCA/TruncatedSVD, metrics
imbalanced-learn>=0.12     # SMOTE and random under-sampling

# --- Natural Language Processing ---
nltk>=3.8                  # tokenisation, stop-words, WordNet lemmatisation
gensim>=4.3                # Word2Vec word embeddings

# --- Visualisation ---
matplotlib>=3.7
seaborn>=0.13

# --- Persistence ---
joblib>=1.3

# ---------------------------------------------------------------------------
# OPTIONAL (BERT vectoriser). Not required to run the experiments; the code
# degrades gracefully when these are absent. Installing them needs ~2 GB and
# network access to the model hub.
#   transformers>=4.40
#   torch>=2.2
# ---------------------------------------------------------------------------
